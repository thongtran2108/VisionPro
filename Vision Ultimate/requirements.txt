PySide6>=6.5.0
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
# ── Optional — OCR Max tool (chọn 1 trong 2 engine, hoặc cả 2 cho 'auto') ──
# 1) Tesseract — nhẹ, cần Tesseract binary + langpack hệ thống:
#      Ubuntu:  sudo apt install tesseract-ocr tesseract-ocr-vie tesseract-ocr-eng
#      Windows: https://github.com/UB-Mannheim/tesseract/wiki
#      macOS:   brew install tesseract tesseract-lang
# pytesseract>=0.3.10
#
# 2) EasyOCR — không cần binary, tốt cho tiếng Việt + nền phức tạp,
#    nhưng nặng (PyTorch ~500MB+, load model lần đầu ~5-15s):
# easyocr>=1.7.0
#
# ── Optional — Barcode/QR (ID Reader tool) ──
# pyzbar>=0.1.9
